{
  "name": "opencode-models-fix",
  "version": "0.1.0",
  "description": "Gestion unificada de fallos de modelos para OpenCode (kiosapi + nvidia + opencode)",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "deploy": "bun scripts/deploy.mjs"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.4.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
